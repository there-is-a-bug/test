#include <iostream>
using namespace std;
typedef long long int ll;
bool init_move(int move[], int n)
{
    for(int i = 0; i < n; i++)
    {
        move[i] = 0;
    }
    return true;
}
int main()
{
    int move[100000];
    ll x_all, y_all, z_all;
    x_all = 0; 
    int n, m, i;
    cin >> n >> m;
    init_move(move, n);
    
    

}